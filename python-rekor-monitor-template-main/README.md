# Python Rekor Monitor 

A couple of notes: 

    1. my initial run of mypy (after running --install-types to get the types for my imports) 
    showed no errors, so both my initial and final mypy txt files are the same. 

    cmds: mypy main.py merkle_proof.py util.py > mypy.initial.txt
          mypy main.py merkle_proof.py util.py > mypy.final.txt  

    2. 




